# OpenAI
OPENAI_API_KEY=sk-xxxx

# Google OAuth (download these from Google Cloud Console when you create OAuth credentials)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Optional: specify where to cache tokens so you don’t re-login each time
GOOGLE_TOKEN_FILE=token.json

